import React, { useState } from "react";
import { Text, View } from "react-native";

const ProfileScreen = () => {
  return (
    <View>
      <Text style={{ fontSize: 25, color: "#000" }}>Profile page</Text>
    </View>
  );
};

export default ProfileScreen;
